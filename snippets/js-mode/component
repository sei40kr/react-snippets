# name : React component template
# contributor : Seong Yong-ju <sei40kr@gmail.com>
# key: component
# --

import { Component } from 'react';

import './`(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`.scss';

type Props = {};

type State = {};

export default class `(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))` extends Component<Props, State> {
  render() {
    return ${0:<div />};
  }
}
