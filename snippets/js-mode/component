# name : React component template
# contributor : Seong Yong-ju <sei40kr@gmail.com>
# key: component
# --

import * as React from 'react';

import './`(react-snippets--file-class-name)`.${1:$$(yas-choose-value '("scss" "css"))}';

const CSS_ROOT = '`(react-snippets--component-css-root)`'

const `(react-snippets--file-class-name)` = (props) => (
  <${2:div} className={CSS_ROOT}>
    $0
  </$2>
);

export default `(react-snippets--file-class-name)`;
