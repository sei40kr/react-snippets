# name : React component template
# contributor : Seong Yong-ju <sei40kr@gmail.com>
# key: component
# --

import './`(react-snippets//file-class-name)`.scss';

const CSS_ROOT = '`(react-snippets//component-css-root)`'

export interface IProps {}

const `(react-snippets//file-class-name)` = (props: IProps) => (
  <${1:div} className={CSS_ROOT}>
    $0
  </$1>
);

export default `(react-snippets//file-class-name)`;